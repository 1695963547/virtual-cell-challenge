以下是 7 项指标的完整对比：

> **重要口径说明**：官方榜单的 **DES** 指标经反推对应 cell-eval 的 `overlap_at_N`，不是 fdr<0.05 的严格集合交集。`overlap_at_N` 按 |log2FC| 对 real/pred 的显著基因排序后取 top k（k=real 显著基因数），计算 `|real_topk ∩ pred_topk| / k`。下文所有 DES 数值均使用这一口径。我们之前手算的 fdr 交集/min（0.37~0.76 量级）与榜单尺度不一致，已不再使用。

## full_run / SE旧 / cs512 / state_lg / tahoe_best vs Generalist 榜第一 cleopatra

| #   | 指标               | 方向     | cleopatra (Generalist 榜 #1) | full_run (best.ckpt) | **SE旧（fewshot）** | cs512 (best.ckpt) | **state_lg (best.ckpt)** | **tahoe_best (best.ckpt)** | 差距分析（最佳 vs cleopatra）        |
| --- | ---------------- | ------ | ---------------------------- | -------------------- | ---------------- | -------------------- | ---------------------- | ------------------------ | ------------------------------- |
| 1   | **DES (=overlap_at_N)** | ↑ 越大越好 | **0.228**        | 0.133                | 0.175          | 0.178                | **0.198**              | 0.195                    | cleopatra 领先 13%  |
| 2   | **PDS**          | ↑ 越大越好 | **0.747**        | 0.526                | 0.521          | 0.531                | 0.560                  | **0.564**                | cleopatra 领先 33%                |
| 3   | **MAE**          | ↓ 越小越好 | **0.086**        | **0.036**            | 0.353          | 0.544                | 0.507                  | 0.504                    | full_run 最强 |
| 4   | **SPEARMAN**     | ↑ 越大越好 | **0.473**        | 0.081                | 0.175          | 0.233                | **0.284**              | 0.176                    | cleopatra 领先 1.7x               |
| 5   | **SPEARMAN_LFC** | ↑ 越大越好 | **0.396**        | 0.093                | 0.135          | 0.229                | 0.146                  | 0.144                    | cleopatra 领先 2.7x |
| 6   | **AUPRC**        | ↑ 越大越好 | **0.266**        | 0.237                | 0.205          | 0.207                | **0.213**              | 0.213                    | cleopatra 领先 25%，full_run 最接近   |
| 7   | **PEARSON**      | ↑ 越大越好 | **0.203**        | 0.091                | 0.073          | 0.099                | 0.051                  | 0.068                    | cleopatra 领先 4x |

> **注**：cleopatra 的 DES/PDS/MAE/SPEARMAN/SPEARMAN_LFC/AUPRC/PEARSON 为**榜单报告值**（可能经过不同的归一化处理）；full_run 和 SE+decoder 为本地 agg_results.csv 的**原始值**（mean 行），两者可能不完全在同一口径下直接可比。

## 官方 3 指标榜（DES / PDS / MAE / Overall）

VCC 官方 Overall 只由 3 项决定：

```
Overall = 1/3 × (DES_scaled + PDS_scaled + MAE_scaled) × 100
        （负值 clipped 到 0）
```

| 队伍 / 模型                  | DES       | PDS       | MAE   | Overall  |
| ------------------------ | --------- | --------- | ----- | -------- |
| **BM_xTVC**（官方 #1）       | **0.349** | **0.872** | 1.026 | **34.0** |
| **XLearning Lab**（官方 #2） | 0.356     | 0.850     | 1.069 | 32.8     |
| **Outlier**（官方 #3）       | 0.361     | 0.844     | 4.219 | 32.6     |
| **state_lg**（本地）         | **0.198** | 0.560     | 0.507 | **6.68*** |
| **tahoe_best**（本地）       | 0.195     | **0.564** | 0.504 | 6.66*    |
| **cs512**（本地）            | 0.178     | 0.531     | 0.544 | 4.16*    |
| **SE旧**（本地，fewshot）    | 0.175     | 0.527     | 0.353 | 3.80*    |
| **full_run**（本地）         | 0.133     | 0.526     | 0.036 | 2.10*    |

> \* 本地模型的 Overall 是按本地 `baseline_train` 归一化分套用同一公式估算：`(DES_norm + PDS_norm + MAE_norm)/3 × 100`。由于本地 MAE 的数值尺度与官方榜单不同（官方 MAE 是 1.x 量级，本地是 0.0x~0.5x 量级），这个 Overall **不能直接和官方 34/32.x 比较**，仅用于本地模型之间的相对排序。

### 3 指标视角下的观察

1. **DES**：本地最好（state_lg ≈ 0.198）是官方 score 榜第 1 名 BM_xTVC（0.349）的 **~57%**；对比 Generalist 榜 #1 cleopatra（0.228）约为 **87%**。
2. **PDS**：本地最好（state_lg ≈ 0.560）是官方 score 榜第 1 名 BM_xTVC（0.872）的 **~64%**；对比 Generalist 榜 #1 cleopatra（0.747）约为 **75%**。
3. **MAE**：full_run 的 0.036 仍然最强；state_lg（0.507）比 cs512（0.544）好 7%，但仍是主要拉分项。
4. **本地相对排序**：**state_lg（6.68）> cs512（4.16）> SE旧（3.80）> full_run（2.10）**，state_lg 主要靠 PDS 和 DES 拉开（PDS_norm 是 cs512 的 2.3x）。

| 维度                         | 结论                                                                                              |
| -------------------------- | ----------------------------------------------------------------------------------------------- |
| **最接近榜首的指标**               | **AUPRC**（full_run 0.237 vs 0.266，仅差 12%）和 **MAE**（full_run 0.036 比 cleopatra 的 0.086 还好 2.4 倍） |
| **最大短板**                   | **SPEARMAN** 和 **SPEARMAN_LFC**——我们的模型在 DE 效果量排序上差距 3~6 倍                                       |
| **PDS 差距**                 | 0.560 vs 0.747，state_lg 把 PDS 从 0.531 拉到 0.560，但仍差 33%                                       |
| **full_run vs SE旧** | full_run 在 MAE/AUPRC/PEARSON 上优；SE旧 在 DES/SPEARMAN/SPEARMAN_LFC 上优                       |
| **cs512 vs SE旧**    | cs512 在 DES/PDS/SPEARMAN/SPEARMAN_LFC/PEARSON 上全面领先；MAE 退步（0.353→0.544），AUPRC 接近持平                  |
| **cs512 vs full_run**      | cs512 的 SPEARMAN/SPEARMAN_LFC 是 full_run 的 2.9x / 2.5x，但 MAE 差 15 倍（过拟合 + best.ckpt 错位）         |
| **state_lg vs cs512**      | state_lg 在 DES/PDS/MAE/SPEARMAN/AUPRC 上领先；SPEARMAN_LFC 和 PEARSON 退步。本地 Overall 6.68 vs 4.16（+61%） |

## 指标映射关系

| 榜单名称         | cell-eval 原始字段            | 含义                             |
| ------------ | ------------------------- | ------------------------------ |
| DES          | `overlap_at_N`            | 预测与真值 top-N DE 基因重叠率           |
| PDS          | `discrimination_score_l1` | L1 距离判断预测最接近哪个真值扰动             |
| MAE          | `mae`                     | 全转录组 18080 基因平均绝对误差            |
| SPEARMAN     | `de_spearman_sig`         | 显著 DE 基因效果量的 Spearman 相关       |
| SPEARMAN_LFC | `de_spearman_lfc_sig`     | log fold change 的 Spearman 相关  |
| AUPRC        | `pr_auc`                  | PR 曲线下面积（DE 基因分类质量）            |
| PEARSON      | `pearson_delta`           | delta 表达（扰动 vs 对照）的 Pearson 相关 |







三个模型的差别一目了然：

## 核心区别

|                  | **full_run**                      | **SE旧（fewshot)**         | **SE新（no_fewshot)**    |
| ---------------- | --------------------------------- | ------------------------ | ---------------------- |
| **输入维度**         | 18080 基因                          | 2058 SE嵌入                | 2058 SE嵌入              |
| **输出方式**         | 直接预测基因                            | 预测嵌入→decoder解码基因         | 预测嵌入→decoder解码基因       |
| **embed_key**    | `null`（读.X）                       | `X_state`（读obsm）         | `X_state`（读obsm）       |
| **output_space** | `gene`                            | `all`                    | `all`                  |
| **数据文件**         | competition_support_set（无X_state） | vci_pretrain（有X_state）   | vci_pretrain（有X_state） |
| **toml**         | starter.toml                      | starter_**fewshot**.toml | starter.toml           |
| **留出扰动**         | 无                                 | **20个扰动留出**（val）         | 无                      |
| **训练扰动**         | 151                               | 131（少了20个）               | 151                    |
| **val**          | 0                                 | 20扰动                     | 0                      |
| **max_steps**    | 40000                             | 8000                     | 8000                   |

## 唯一变量（公平对比）

SE新 vs full_run 的**唯一差异**就是：

- **输入表示**：18080 原始基因 vs 2058 SE嵌入
- **输出方式**：直接预测 vs decoder 解码
- **max_steps**：40000 vs 8000（full_run 训练了 5 倍的步数）

数据、toml、训练集（221,273 细胞）、细胞类型完全一样。

## SE旧 vs SE新：同口径结果对比

两者都是 SE+decoder 路线（`output_space=all`、`embed_key=X_state`），唯一区别是 **训练时是否留出 20 个 H1 扰动做验证集**：

| 指标                         | 方向     | SE旧（fewshot，131 训练扰动） | SE新（no_fewshot，151 训练扰动） | 变化         |
| -------------------------- | ------ | --------------------- | ------------------------ | ---------- |
| **DES (=overlap_at_N)**      | ↑ 越大越好 | 0.175                 | **0.178**                | +2%        |
| **PDS**                    | ↑ 越大越好 | 0.527                 | **0.531**                | +1%        |
| **MAE**                    | ↓ 越小越好 | **0.353**             | 0.544                    | -54%       |
| **SPEARMAN**               | ↑ 越大越好 | 0.067                 | **0.233**                | **+247%**  |
| **SPEARMAN_LFC**           | ↑ 越大越好 | 0.020                 | **0.230**                | **+1040%** |
| **AUPRC**                  | ↑ 越大越好 | 0.200                 | **0.207**                | +3%        |
| **PEARSON**                | ↑ 越大越好 | 0.042                 | **0.099**                | **+136%**  |

> **VCC Overall 计算方式**：DES 使用官方口径 `overlap_at_N`，baseline=0.1065；PDS baseline=0.509；MAE baseline=0.026。套用 `Overall = (DES_scaled + PDS_scaled + MAE_scaled)/3 × 100`，负值 clip 到 0。
>
> 结果：SE新 Overall = **4.16**，SE旧 Overall = **3.80**。

### 关键结论

1. **多用的 20 个 H1 扰动对 DES 提升很小**（0.175 → 0.178，+2%），对 SPEARMAN/SPEARMAN_LFC 提升显著（+247% / +1040%）。这说明留出验证虽然减少了训练扰动，但它同时让模型失去了学习这些真实 H1 扰动响应模式的机会，主要影响的是 DE 效果量排序而非 top-N 基因重叠。
2. **PDS 几乎没动**（0.527 → 0.531）：扰动可区分性塌缩的问题，不是 20 个扰动能解决的。
3. **MAE 明显变差**（0.353 → 0.544）：训练扰动变多后，模型更难拟合精确幅度，predicted delta 的幅度被放大。
4. **综合看 SE新 略优**：VCC Overall 4.16 vs 3.80，提升主要来自 PDS 和 SPEARMAN 类指标。对于 2025 榜单，**no_fewshot（151 训练扰动）是更好的设置**。

## 为什么 SE新 MAE 差这么多？

SE 新/旧的 MAE（0.544 / 0.353）都远差于 full_run（0.036），说明 **decoder 解码误差是主要瓶颈**——模型先预测 2058 维嵌入，再解码回 18080 维基因，两层误差叠加。而 full_run 直接预测基因，没有信息瓶颈。

但 SE 路线的 **DES（=overlap_at_N）反而最高**（0.178 > 0.175 > 0.133），说明嵌入空间的预测在“哪些基因差异表达”这个任务上更准——可能 SE 嵌入滤掉了噪声，保留了扰动相关的信号。

## cs512：cell_set_len=512 训练

在 SE+decoder (SE新 no_fewshot) 基础上，把每个 "sentence" 的细胞数从默认值（full cells）降到 512，并优化了 dataloader（`num_workers=64, prefetch_factor=8`）。

|                     | SE+decoder (SE新) | **cs512**                   | 差异                          |
| ------------------- | ---------------- | --------------------------- | --------------------------- |
| **输入维度**            | 2058 SE嵌入        | 2058 SE嵌入                   | 同                           |
| **输出方式**            | 嵌入→decoder解码     | 嵌入→decoder解码                | 同                           |
| **cell_set_len**    | 128（默认）          | **512**                     | 每 step 采 4x 细胞数             |
| **max_steps**       | 8000             | 8000                        | 同                           |
| **num_workers**     | 16               | 64                          | 提速约 1.5~2x                  |
| **prefetch_factor** | 4                | 8                           | 缓解 HDF5 抖动                  |
| **训练耗时**            | ~1.8h（16w）       | ~11h（64w）                   | **慢 6 倍**（即便 worker 开到 4 倍） |
| **val_loss 最优步**    | —                | step 6000                   | 之后过拟合，val_loss 持续上升         |
| **best.ckpt 实际**    | —                | step-7000 权重 + step-6000 分数 | **错位 bug**，代码已修复            |

### 榜单口径归一化分（vs baseline_train）

| run             | avg_score  | 备注                  |
| --------------- | ---------- | ------------------- |
| **state_lg**    | **0.0472** | 目前最高                |
| **tahoe_best**  | 0.0462     | 与 state_lg 基本持平     |
| **cs512**       | 0.0368     | 第三                  |
| full_run        | 0.0332     | 历史最好（8/3）           |
| 7/31 best       | 0.0330     | 早期 SE 路线 checkpoint |

### tahoe_best：官方选优配置（8000 步）

`MODEL=tahoe_best`（201M 参数：4 层 backbone，hidden_dim=1440，predict_residual=True），其余配置与 state_lg 完全对齐（cell_set_len=512，8000 步）。训练 val_loss 0.695→0.262 单调下降无过拟合，且最终 val_loss 比 state_lg（0.351）低 25%。

| 指标（原始值） | cs512 | state_lg | tahoe_best | tahoe_best vs state_lg |
|---|---|---|---|---|
| DES | 0.178 | **0.198** | 0.195 | -2% |
| PDS | 0.531 | 0.560 | **0.564** | +0.7% |
| MAE | 0.544 | 0.507 | **0.504** | 略好 |
| SPEARMAN | 0.233 | **0.284** | 0.176 | **-38%** |
| SPEARMAN_LFC | 0.229 | **0.146** | 0.144 | -2% |
| AUPRC | 0.207 | 0.213 | 0.213 | 持平 |
| PEARSON | 0.099 | 0.051 | **0.068** | +34% |
| **avg_score** | 0.0368 | **0.0472** | 0.0462 | -2% |

**关键观察**：

1. **tahoe_best ≈ state_lg**：avg_score 0.0462 vs 0.0472，基本持平。tahoe_best 在 PDS/MAE/PEARSON 上略优，但 SPEARMAN 大幅退步（0.176 vs 0.284）。
2. **val_loss 与榜单指标再次脱钩**：tahoe_best 的 val_loss（0.262）比 state_lg（0.351）低 25%，但测试指标几乎一样。进一步证实 hepg2 zeroshot 的 val_loss 不是 VCC 测试表现的可靠代理。
3. **换模型配置不是出路**：state（108M）→ state_lg（301M）→ tahoe_best（201M）三个配置下，PDS 都卡在 0.53~0.56。瓶颈在扰动多样性不足（PDS 塌缩），不在模型容量。

### state_lg 与 cs512 的逐项对比

| 指标（原始值） | cs512 | state_lg | 变化 |
|---|---|---|---|
| DES | 0.178 | **0.198** | +11% |
| PDS | 0.531 | **0.560** | +5% |
| MAE | 0.544 | **0.507** | +7%（更好） |
| SPEARMAN | 0.233 | **0.284** | +22% |
| SPEARMAN_LFC | **0.229** | 0.146 | -36% |
| AUPRC | 0.207 | **0.213** | +3% |
| PEARSON | **0.099** | 0.051 | -48% |
| **avg_score** | 0.0368 | **0.0472** | **+28%** |

### 观察

1. **DE 类指标大幅提升**：cs512 的 SPEARMAN (0.233) 是 SE+decoder (0.175) 的 1.3x、是 full_run (0.081) 的 2.9x；SPEARMAN_LFC (0.229) 是 SE+decoder (0.135) 的 1.7x。说明更短的 sentence 强迫模型在少量细胞内识别扰动信号，提升了「找出受影响基因并排序」的能力。
2. **state_lg 进一步把 DES/PDS/SPEARMAN 推上去**，尤其是 PDS 从 0.531 到 0.560，说明大模型对扰动特异性的区分能力更强。但 SPEARMAN_LFC 和 PEARSON 退步，可能是大模型对表达量幅度的拟合更不稳定。
3. **MAE 略有改善**：state_lg 0.507 比 cs512 0.544 好 7%，但仍远差于 full_run。
4. **综合分创新高**：state_lg 的 avg_score 0.0472 比 cs512 的 0.0368 高 28%，主要因为 PDS 和 DES 的 normalized 提升更大。
5. **训练更稳定**：state_lg 的 val_loss 从 step 999 的 0.665 持续下降到 step 7999 的 0.351，没有出现 cs512 那种 step-6000 后过拟合上升的情况。

## cs256_energy：cell_set_len=256 消融（2026-08-11）

`state_emb_all_lg_cs256_energy`：与 state_lg（cs512）同配置同 loss（energy），**唯一区别 cell_set_len 512→256**，从头训练 8000 步（无 init_from、无冻结）。目的：隔离 cs 变化对 SE latent + energy loss 路线的影响，与 delta_mse_cs256 组成 cs × loss 消融矩阵。

### 7 指标原始值对比（@ test）

| 指标 | 方向 | state_lg（cs512） | cs256_energy | 变化 |
|---|---|---|---|---|
| **DES** | ↑ 越大越好 | 0.198 | **0.202** | +2% |
| **PDS** | ↑ 越大越好 | **0.560** | 0.547 | -2% |
| **MAE** | ↓ 越小越好 | **0.507** | 0.512 | -1%（持平） |
| **SPEARMAN** | ↑ 越大越好 | **0.284** | 0.068 | **-76%** |
| **SPEARMAN_LFC** | ↑ 越大越好 | 0.146 | **0.179** | **+23%** |
| **AUPRC** | ↑ 越大越好 | 0.213 | **0.215** | +1% |
| **PEARSON** | ↑ 越大越好 | 0.051 | **0.081** | **+59%** |
| **avg_score** | — | **0.0472** | 0.0386 | -18% |

### 归一化分（vs baseline_train）

| 指标 | state_lg | cs256_energy |
|---|---|---|
| DES_norm | 0.0961 | 0.0968 |
| PDS_norm | 0.1042 | 0.0762 |
| SPEARMAN_norm | — | 0.1966 |
| AUPRC_norm | — | 0.0835 |
| MAE_norm | 0.0 | 0.0 |
| **avg_score** | **0.0472** | 0.0386 |

### 关键观察

1. **cs 512→256 总体影响很小**：DES/PDS/MAE 几乎不变（±2%），avg_score 0.0386 vs 0.0472 的差距主要来自 SPEARMAN 暴跌（0.284→0.068，-76%）。
2. **SPEARMAN 大幅退步但 SPEARMAN_LFC/PEARSON 反而提升**（+23%/+59%）：256 细胞/sentence 的信息量不足以稳定排序显著 DE 基因，但幅度类指标变好——cs 影响的是 DE 排序稳定性而非表达幅度。
3. **2×2 消融结论（cs × loss）**：cs256 下 energy→delta_mse 带来 DES +17%（0.202→0.236）、MAE -72%（0.512→0.142），而 cs 512→256 本身几乎无影响 → **提升主要来自 loss 改变，而非 cs 变小**。
4. **注意 delta_mse_cs256 额外叠加了 init_from=cs512 best.ckpt + freeze_main**，loss 增益与初始化/冻结效应混叠，若要完全解耦需补跑"cs256 + delta_mse + 从头训练"。

## valsplit：官方 validation 当训练时验证集（vs state_lg 老路线）

`state_emb_all_lg_cs512_valsplit` 与 state_lg 同配置（301M 参数、cell_set_len=512、8000 步），**唯一区别是训练时 val 从 hepg2 zeroshot 换成官方 validation split（50 扰动）**，best.ckpt 按该 val 的 val_loss 选出。08-09 晚用 adata_Test.h5ad（100 扰动）评测。

### 7 指标原始值对比（@ test）

| 指标                 | 方向     | state_lg（老路线） | valsplit       | valsplit vs state_lg |
| -------------------- | -------- | ---------------- | -------------- | -------------------- |
| **DES**              | ↑ 越大越好 | 0.198            | **0.199**      | +0.5%（基本持平）       |
| **PDS**              | ↑ 越大越好 | **0.560**        | 0.533          | -5%                  |
| **MAE**              | ↓ 越小越好 | **0.507**        | 0.537          | -6%（更差）              |
| **SPEARMAN**         | ↑ 越大越好 | **0.284**        | 0.165          | **-42%**             |
| **SPEARMAN_LFC**     | ↑ 越大越好 | **0.146**        | 0.099          | **-32%**             |
| **AUPRC**            | ↑ 越大越好 | 0.213            | **0.217**      | +2%                  |
| **PEARSON**          | ↑ 越大越好 | **0.051**        | 0.019          | **-63%**             |

### 归一化分与 Overall 估算

| 指标                | state_lg  | valsplit   |
| ------------------- | --------- | ---------- |
| DES_norm            | 0.0961    | 0.0935     |
| PDS_norm            | 0.1042    | 0.0475     |
| MAE_norm            | 0.0       | 0.0        |
| **avg_score**       | **0.0472** | **0.0373** |
| **Overall 估算**  | **6.68**  | **4.70**   |

### 关键观察

1. **DES 几乎一样**（0.199 vs 0.198）：top-N 基因重叠率对 val_loss 口径不敏感。
2. **PDS 明显退步**（0.533 vs 0.560，-5%）：val_loss 与 PDS 脱钩的又一例证（val_loss 选 ckpt 并不优化 PDS）。
3. **SPEARMAN / SPEARMAN_LFC / PEARSON 大幅退步**（-42% / -32% / -63%）：val_loss（latent energy distance）与 DE 效果量排序/方向一致性脱钩已充分证实。
4. **AUPRC 略好**（0.217 vs 0.213）：DE 基因分类质量微提升。
5. **Overall 从 6.68 跌到 4.70**（-30%）：主要 PDS_norm 腰斩拖累。
6. **val_loss 与榜单指标脱钩再确认**：valsplit 用官方 validation val_loss 选 ckpt，但 test 上全面弱于用 hepg2 val_loss 选 ckpt 的 state_lg。这印证了 STATUS 4.4 节的分析——val_loss 算的是 latent 空间的 energy distance，与 VCC 的 DES/PDS/MAE 不在同一度量空间。

## Route B 实验：绕过 SE latent 瓶颈 + 改 loss

### 实验背景与路线

state_lg 的瓶颈是 SE latent 信息瓶颈（18080→2058→18080）和 latent energy distance loss 与基因空间 DES/PDS 指标脱钩。Route B 同时验证两条修复路线：

| 实验 | 路线 | 模型 | embed_key | output_space | loss | optimizer | cell_set_len | 步数 | 状态 |
|---|---|---|---|---|---|---|---|---|---|
| **delta_mse_cs256** | SE latent + delta MSE decoder | state_lg (301M) | X_state (SE) | all | 主loss=energy, decoder=**delta_mse** (冻结主网络) | Adam | 256 | 8000 | 已完成 |
| **state_fused_run2** | 基因空间 + fused PDS/DES proxy | state (151M) | null (raw counts) | gene | **fused** (wmse+pds+dir) | Adam | 128 | 8000 | 已完成 |
| **state_lg_fused_adamw** | 基因空间 + fused + 大模型 + AdamW | state_lg (301M) | null (raw counts) | gene | **fused** (wmse+pds+dir) | **AdamW** | 512 | 8000 | 已完成 |

**delta_mse_cs256 路线**：保留 SE latent 架构（state_lg, embed_key=X_state），从 state_lg best.ckpt 初始化，冻结主网络（transformer/encoders, 288M 参数），只训练 gene_decoder（13M 参数），将 decoder loss 从 energy distance 替换为 delta MSE（pseudo-bulk delta 的均方误差）。

**state_fused_run2 路线**：完全绕过 SE latent（embed_key=null, output_space=gene），直接在 18080 维基因空间端到端训练，使用自研 FusedPDSDESLoss（weighted_mse DES 代理 + contrastive_pds PDS 代理 + direction_loss 方向一致性），与 full_run 唯一区别是 loss 函数。

**state_lg_fused_adamw 路线**：在 fused_run2 基础上升级到 state_lg 大模型（301M）、cs512、AdamW 优化器（weight_decay=0.01），验证大模型容量 + 更好的优化器是否能进一步提升。

### 7 指标原始值对比（@ test）

| 指标 | 方向 | state_lg (参考) | delta_mse_cs256 | vs state_lg | state_fused_run2 | vs state_lg | lg_fused_adamw | vs state_lg |
|---|---|---|---|---|---|---|---|---|
| **DES** | ↑ 越大越好 | 0.198 | **0.236** | **+19%** | 0.199 | +0.5% | 0.214 | +8% |
| **PDS** | ↑ 越大越好 | **0.560** | 0.526 | -6% | 0.545 | -3% | 0.508 | -9% |
| **MAE** | ↓ 越小越好 | 0.507 | 0.142 | -72% | 0.114 | -77% | **0.076** | **-85%** |
| **SPEARMAN** | ↑ 越大越好 | **0.284** | 0.263 | -7% | 0.154 | -46% | 0.046 | **-84%** |
| **SPEARMAN_LFC** | ↑ 越大越好 | 0.146 | **0.194** | **+33%** | 0.089 | -39% | 0.097 | -34% |
| **AUPRC** | ↑ 越大越好 | 0.213 | 0.220 | +3% | **0.234** | +10% | 0.220 | +3% |
| **PEARSON** | ↑ 越大越好 | 0.051 | 0.045 | -12% | **0.085** | **+67%** | 0.053 | +4% |

### 归一化分对比

| 指标 | state_lg | delta_mse_cs256 | state_fused_run2 | lg_fused_adamw |
|---|---|---|---|---|
| overlap_at_N (DES norm) | 0.198 | 0.136 | 0.093 | 0.117 |
| discrimination_score_l1 (PDS norm) | 0.560 | 0.034 | 0.072 | 0.0 |
| de_spearman_sig (SPEARMAN norm) | 0.284 | 0.365 | 0.271 | 0.0 |
| pr_auc (AUPRC norm) | 0.213 | 0.220 | 0.105 | 0.034 |
| pearson_delta (PEARSON norm) | 0.051 | 0.0 | 0.0 | 0.004 |
| **avg_score** | **0.0472** | 0.0417 | 0.0488 | **0.0327** |

> 归一化分低的根因：baseline（训练集扰动均值）本身 PDS 已达 ~0.49，模型 0.53 vs baseline 0.49 的增量仅 0.03，归一化后接近 0。而 state_lg 的 PDS 0.56 增量更大，因此归一化分更高。这是 baseline 归一化方法的固有局限。

### 关键观察

1. **delta_mse 的 DES 0.236 是目前所有实验最高**（超过 state_lg 0.198 的 19%），且 MAE 从 0.507 降到 0.142（好 3.6 倍）。说明 delta MSE decoder loss 确实改善了 DE 基因检出和表达量精度。
2. **delta_mse 的 PDS 0.526 比 state_lg 0.560 低 6%**，但原始值差距很小。delta MSE 改善了 DES/MAE 但略微牺牲了 PDS。
3. **fused_run2 的 MAE 0.114 是所有实验最低**（比 state_lg 好 4.5 倍），且 PEARSON 0.085 最高（+67%）。基因空间直接训练显著改善了表达量预测精度。
4. **fused_run2 的 DES 0.199 与 state_lg 持平**，但 SPEARMAN 0.154 大幅退步（-46%），说明 fused loss 的 weighted_mse 分量优化了 top-N 重叠但没有改善 DE 效果量排序。
5. **两个实验的扰动区分度分析**：不同扰动预测的 cosine 相似度 delta_mse=0.9998 / fused_run2=0.9985（state_lg ~0.96），说明基因空间训练的扰动特异性弱于 SE latent 路线。
6. **lg_fused_adamw 的 MAE 0.076 是所有实验最低**（比 state_lg 好 6.7 倍），但 SPEARMAN 0.046 暴跌（-84%），avg_score 0.0327 也是所有实验最低。大模型 + AdamW 进一步放大了基因空间 fused loss 的 trade-off：MAE 更强但排序能力几乎丧失。
7. **综合判断**：delta_mse 在 DES/MAE 上有突破性提升且 SPEARMAN 仅退步 7%，是三个基因空间实验中最好的平衡点。如果能在保持 DES 提升的同时提高 PDS，有可能超过 state_lg。

### FusedPDSDESLoss 公式

```
total_loss = w_mse × weighted_mse + w_pds × contrastive_pds + w_dir × direction_loss

weighted_mse = mean((1 + α × |δ_true|/mean(|δ_true|)) × (δ_pred - δ_true)²)    # DES 代理：DE 基因加权
contrastive_pds = CrossEntropy(-L1_dist_matrix/τ, identity_labels)             # PDS 代理：batch 内对比学习
direction_loss = mean(1 - cos(δ_pred, δ_true))                                   # 方向一致性

当前默认权重: w_mse=1.0, w_pds=0.1, w_dir=0.1, α=5.0, τ=10.0
```

## FCN Pseudo-bulk：XLearning 式轻量训练（2026-08-11）

### 实验背景

参照 VCC 2025 亚军 XLearning Lab 方案，搭建 FCN pseudo-bulk pipeline：
- **输入**：ctrl_mean (18080) + ESM-2 蛋白嵌入 (5120) = 23200 维
- **输出**：delta (18080)，残差学习
- **Loss**：MSE + 0.1 × direction_loss（cosine）
- **优化器**：AdamW(lr=1e-3, weight_decay=0.05)
- **训练数据**：6 个细胞系 × 扰动 = 590 个 pseudo-bulk 样本（deltas_*.npz）
- **模型**：PerturbFCN(hidden=2048, layers=3, dropout=0.15)，88.8M 参数
- **训练耗时**：109 秒（500 epochs，best epoch=417）

### 7 指标原始值对比（@ test）

| 指标 | 方向 | state_lg (参考) | **FCN** | vs state_lg | full_run | lg_fused_adamw |
|---|---|---|---|---|---|---|
| **DES** | ↑ 越大越好 | **0.198** | 0.129 | **-35%** | 0.133 | 0.214 |
| **PDS** | ↑ 越大越好 | **0.560** | 0.522 | -7% | 0.526 | 0.508 |
| **MAE** | ↓ 越小越好 | 0.507 | **0.083** | **-84%** | 0.036 | 0.076 |

### 归一化分（vs baseline_train）

| 指标 | state_lg | FCN |
|---|---|---|
| DES norm | 0.0961 | 0.0144 |
| PDS norm | 0.1042 | 0.0251 |
| MAE norm | 0.0 | 0.0 |
| **Overall (VCC 3-metric)** | **6.68** | **1.31** |

### 关键观察

1. **MAE 大幅改善**（0.083 vs state_lg 0.507，-84%）：FCN 预测的表达量非常接近真值，与 cleopatra（0.086）相当。但 MAE 仍高于 baseline（0.026），归一化分仍为 0。
2. **DES/PDS 均弱于 state_lg**：DES 0.129 接近 full_run（0.133），PDS 0.522 也类似。FCN 预测了"平滑"的扰动响应——MAE 好但扰动区分度不足。
3. **与统计迁移路线对比**：FCN MAE(0.083) ≈ 迁移 x1 MAE(0.087)，PDS 也接近（0.522 vs 0.534）。FCN 本质上学到了类似"均值回归"的映射，而非扰动特异性响应。
4. **根因**：590 个训练样本 + 88.8M 参数 → 强正则化 → 平滑输出。ESM-2 提供了蛋白特征但不足以让 FCN 区分 100 个未见扰动的精细响应模式。
5. **潜在改进方向**：
   - Delta 放大（统计迁移已验证 ×64 有效提升 PDS 0.534→0.576）
   - 加入 replogle_full（9866 k562 样本）增加训练数据
   - 对比学习损失（contrastive）强制扰动特异性

### 文件路径

| 文件 | 路径 |
|---|---|
| 数据构建 | `/home/zjh/state-vcc-local/pseudobulk/build_fcn_data.py` |
| 训练脚本 | `/home/zjh/state-vcc-local/pseudobulk/train_fcn.py` |
| 推理脚本 | `/home/zjh/state-vcc-local/pseudobulk/infer_fcn.py` |
| Pipeline 脚本 | `/home/zjh/state-vcc-local/run_fcn_pipeline.sh` |
| Checkpoint | `/home/zjh/state-vcc-local/pseudobulk/fcn_best.pt` |
| 预测 h5ad | `/home/zjh/state-vcc-local/state/competition/prediction_test_fcn.h5ad` |
| 评测结果 | `/home/zjh/state-vcc-local/state/competition/vcc_test_eval_fcn/` |
