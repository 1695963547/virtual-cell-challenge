# VCC 技术复盘：单细胞扰动响应预测的复现、优化与思考

> 作者：zhangjianhao
> 时间：2026-08
> 项目：Virtual Cell Challenge（Arc Institute）参赛复现
> 代码：本仓库（scDFM 与 state-vcc-local 两条路线）

---

## 1. 任务背景

**Virtual Cell Challenge (VCC)** 的目标是构建一个能预测基因扰动结果的"虚拟细胞"。给定：

- **control 细胞**：一批未扰动的单细胞转录组（RNA 表达）
- **一个基因扰动**：某个基因的 CRISPRi 敲低（perturbation）

需要预测扰动后细胞的**基因表达分布**（而非单个均值向量），并需要**跨细胞系泛化**（training set 与 test set 的细胞系不同，完全 unseen）。

这是单细胞 + 生成模型的典型交叉任务：既要表达幅度准（MAE），又要扰动效应可区分（DES/PDS）。2025 年榜单前 20 里一半以上来自中国队伍，竞争极其激烈。

## 2. 两条技术路线

复现并适配了两个前沿模型，思路完全不同：

### 路线一：scDFM（Flow Matching）

- 来源：ICLR 2026 开源模型，Distributional Flow Matching（条件生成模型）
- 结构：encoder 把 control 表达 + 扰动编码为 condition，再用 flow matching 生成目标分布
- 扰动编码：flow-fusion + crisper 双分支
- VCC 适配：需解决跨细胞系泛化（训练 scT5 细胞系嵌入）、单基因扰动、全基因维度预测

### 路线二：STATE（Transformer + SE 嵌入）

- 来源：Arc Institute 开源（`state` 仓库），Sequence Embedding (SE) 模型
- 结构：SE-600M 嵌入 18080 个基因 → Transformer encoder → 解码回基因空间
- 双变体：**基因空间直训**（18080 维）与 **SE latent + decoder**（18080→2058→18080）
- 本地最佳结果（Overall 估算 6.68）来自 301M 参数版

## 3. 工程优化（H20 × 4 卡）

这一层的工作量占比最大，也是最有复用价值的部分。

### 3.1 bf16 混合精度：训练加速 2.6-2.8×

H20 的 FP32 算力弱（FP32 ~1 TFLOPS 级），而 BF16 tensor core 极强。把训练切到 bf16 后实测：

- 训练整体加速 **2.6-2.8×**
- 配合 BF16 小 loss 阈值调优，训练更稳定
- 模型结构支持数学等价的 bf16 改写（见下）

### 3.2 18080 全基因下的注意力显存优化

原版 STATE 在训练时用了**基因采样**（每步只训练一部分基因），避免显存爆炸；但这样会引入采样噪声，且无法覆盖全部基因的扰动效应。

目标：训练全基因（18080 维）注意力层。显存估算：
- 18080 × 18080 的注意力矩阵 ≈ 327M 个元素，单卡 FP32 即 ~1.3GB，多头堆叠后随 batch 放大到 **~500GB**，必然 OOM。

**解法**：利用 matmul 的线性性，把注意力计算改为**数学等价的张量拆分**：
- QK^T → 分块计算，softmax 后用等价变换合并
- 避免一次性 materialize 全尺寸注意力矩阵，显存从 ~500GB 降到单卡可用范围

这是纯数学改写，**结果与原实现逐位等价**，不引入任何精度损失，是本次工程上最有含金量的改动。

### 3.3 多卡并行评测：消除 3× 浪费

原版评测逻辑把测试集 3 个 cell line 拆到 3 张卡**各跑全量**，浪费严重。改为按扰动（perturbation）分片到各 rank，每 rank 只算自己的子集，再合并。

- 评测时间接近线性缩短（扰动数 / 卡数）
- 每 rank 独立评测、分别写文件，天然支持断点续跑

### 3.4 训练层其余改进（13 处）

- checkpoint 每 epoch 自动保存 + 断点恢复
- 训练循环显式 break + 汇总
- 动态学习率调度适配
- 完整训练/评测日志与指标落盘

## 4. 实验设计与消融

### 4.1 三条实验路线

| 路线 | 描述 | 结果定位 |
|---|---|---|
| **state_lg** | STATE 301M，全基因直训 | 本地 Overall 最佳（6.68 估算） |
| **state_se** | SE latent + decoder 双阶段 | DES/PDS 略逊，MAE 更好 |
| **scDFM** | Flow Matching 条件生成 | MAE / SPEARMAN_LFC 全模型最强 |
| **pseudo-bulk FCN** | XLearning 式轻量回归 | 作为基准验证（MAE 0.051） |

### 4.2 自研 FusedPDSDESLoss

在加权 MSE 基础上引入 PDS 的对比学习代理 + 方向一致性正则，融合成单 loss。效果（`delta_mse`）：

- **DES 0.236**（所有实验最高，比 state_lg +19%）
- 说明：PDS 的对比约束确实能放大扰动特异性，代价是 MAE 略升

### 4.3 关键消融

- **模型容量 108M → 201M → 301M**：PDS 始终卡在 0.53~0.56。结论：**瓶颈不在容量，在训练数据中扰动多样性不足**
- **cs × loss 消融矩阵**：验证 cell-line 分层采样与 loss 组合的相互影响
- **baseline 对照**：均值模型 MAE ≈ 0.026，几乎无法被超越——解释了榜单 MAE 普遍趋于 0 的现象

## 5. 指标深入分析：DES / PDS / MAE 的权衡

这是整个复现过程中最有价值的认知收获，也解释了榜单现象。

### 5.1 指标本质

| 指标 | 全称/对应 | 测什么 | cell-eval 对应 |
|---|---|---|---|
| **DES** | Differential Expression Similarity | 预测的 DE 基因集合与真实 DE 基因的吻合度 | `overlap_at_N` |
| **PDS** | Perturbation Discriminability Score | 不同扰动之间的可区分度 | `discrimination_score_l1` |
| **MAE** | Mean Absolute Error | 表达幅度的绝对误差 | `mae` |

### 5.2 fundamental trade-off

- **MAE 几乎退出竞争**：均值 baseline 已到 ~0.026，逼近下界；榜单顶尖队伍 MAE_scaled 全为 0
- **DES/PDS 需要"放大扰动响应"**：预测分布要与 control 显著不同（好 PDS）且差异落在真实 DE 基因上（好 DES），这会推高表达幅度 → MAE 变差
- 因此 **"提升 DES/PDS 必然牺牲 MAE"** 是结构性矛盾，榜首队伍（BM_xTVC，DES 0.349 / PDS 0.872 但 MAE 1.026）全部选择了放大扰动响应

### 5.3 对自身结果的解释

- state_lg：DES 0.198 / PDS 0.560 —— 平衡但保守，扰动响应偏小
- scDFM：MAE 0.042 / SPEARMAN_LFC 0.388 —— 幅度准、排序好，但扰动特异性不足
- **结论**：距离榜首（DES 0.349 / PDS 0.872）的主要差距在**扰动特异性的放大与校准**，而非基础生成能力

## 6. 结果汇总

| 模型 | DES↑ | PDS↑ | MAE↓ | SPEARMAN_LFC↑ | 说明 |
|---|---|---|---|---|---|
| state_lg (301M) | 0.198 | 0.560 | 0.507 | 0.190 | 本地 Overall 最佳 |
| delta_mse | **0.236** | 0.526 | 0.142 | — | 自研 loss，DES 最高 |
| scDFM iter15000 | 0.084 | 0.503 | **0.042** | **0.388** | MAE/排序最强 |
| pseudo-bulk FCN | — | — | 0.051 | — | 轻量基准 |
| BM_xTVC（官方 #1） | 0.349 | 0.872 | 1.026 | — | 榜单参考 |
| cleopatra（官方 Gen #1） | 0.228 | 0.747 | 0.086 | — | 榜单参考 |

> 注：本地 Overall 为基于 DES/PDS/MAE 的估算，与官方计分（含 unseen cell line）口径不完全一致，仅作相对参考。完整 7 项指标见 `结果对比.md`。

## 7. 经验教训

1. **先理解指标，再优化模型**。如果一开始就意识到 DES/PDS 与 MAE 的 trade-off，会少走一半弯路——很多"优化"实际是在指标间搬运分数。
2. **工程优化优先级高于模型改动**。bf16（2.6-2.8×）、显存改写、并行评测这些工程项带来的收益（时间 ×3、显存 ÷数十）远大于模型容量的边际提升。
3. **容量不是万能药**。301M 相比 108M 的提升集中在个别指标，扰动多样性（数据侧）才是真正的瓶颈——这提示下一步应聚焦数据增强/更多扰动样本。
4. **两个模型的互补性很强**：scDFM 赢幅度，STATE 赢特异性。正确的方向不是二选一，而是**融合两者的优势**（例如用 scDFM 的输出幅度校准 STATE 的扰动响应）。

## 8. 未来方向

- **扰动响应校准**：针对 PDS 的对比学习正则已有正效果（DES +19%），可继续调优权重与负样本构造
- **两模型融合**：幅度（scDFM）+ 特异性（STATE）的 score-level 融合
- **数据侧增强**：利用官方提供的更多扰动组合、伪扰动构造，提升扰动多样性
- **分布评估对齐**：复刻官方 DES/PDS 计算逻辑，让本地优化与榜单口径完全一致
