# vci_pretrain 版 starter.toml：路径指向 vci_pretrain（含 obsm/X_state），
# 但不留出扰动（与 competition_support_set/starter.toml 完全一致），
# 保证 SE+decoder 与 full_run 公平 A/B 对比（唯一变量 = output_space + embed_key）。
#
# 训练时需传 data.kwargs.embed_key="X_state" data.kwargs.output_space=all

[datasets]
replogle_h1 = "/home/zjh/state-vcc-local/state/vci_pretrain/{competition_train,k562_gwps,rpe1,jurkat,k562,hepg2}.h5"

[training]
replogle_h1 = "train"

[zeroshot]
"replogle_h1.hepg2" = "test"

[fewshot]
